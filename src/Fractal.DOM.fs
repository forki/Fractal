namespace Fractal

open System
open FunScript
open FunScript.TypeScript
open FunScript.TypeScript.React

[<ReflectedDefinition>]
[<AutoOpen>]
module DOM =

    [<AbstractClass; Sealed>]
    type DOM () =
        static member a (p, [<ParamArray>] c) = Globals.createElement ("a", p, c )
        static member abbr (p, [<ParamArray>] c) = Globals.createElement ("abbr", p, c )
        static member address (p, [<ParamArray>] c) = Globals.createElement ("address", p, c )
        static member area (p, [<ParamArray>] c) = Globals.createElement ("area", p, c )
        static member article (p, [<ParamArray>] c) = Globals.createElement ("article", p, c )
        static member aside (p, [<ParamArray>] c) = Globals.createElement ("aside", p, c )
        static member audio (p, [<ParamArray>] c) = Globals.createElement ("audio", p, c )
        static member b (p, [<ParamArray>] c) = Globals.createElement ("b", p, c )
        static member base' (p, [<ParamArray>] c) = Globals.createElement ("base", p, c )
        static member bdi (p, [<ParamArray>] c) = Globals.createElement ("bdi", p, c )
        static member bdo (p, [<ParamArray>] c) = Globals.createElement ("bdo", p, c )
        static member blockquote (p, [<ParamArray>] c) = Globals.createElement ("blockquote", p, c )
        static member body (p, [<ParamArray>] c) = Globals.createElement ("body", p, c )
        static member br (p, [<ParamArray>] c) = Globals.createElement ("br", p, c )
        static member button (p, [<ParamArray>] c) = Globals.createElement ("button", p, c )
        static member canvas (p, [<ParamArray>] c) = Globals.createElement ("canvas", p, c )
        static member caption (p, [<ParamArray>] c) = Globals.createElement ("caption", p, c )
        static member cite (p, [<ParamArray>] c) = Globals.createElement ("cite", p, c )
        static member code (p, [<ParamArray>] c) = Globals.createElement ("code", p, c )
        static member col (p, [<ParamArray>] c) = Globals.createElement ("col", p, c )
        static member colgroup (p, [<ParamArray>] c) = Globals.createElement ("colgroup", p, c )
        static member datalist (p, [<ParamArray>] c) = Globals.createElement ("datalist", p, c )
        static member dd (p, [<ParamArray>] c) = Globals.createElement ("dd", p, c )
        static member del (p, [<ParamArray>] c) = Globals.createElement ("del", p, c )
        static member details (p, [<ParamArray>] c) = Globals.createElement ("details", p, c )
        static member dfn (p, [<ParamArray>] c) = Globals.createElement ("dfn", p, c )
        static member dialog (p, [<ParamArray>] c) = Globals.createElement ("dialog", p, c )
        static member div (p, [<ParamArray>] c) = Globals.createElement ("div", p, c )
        static member dl (p, [<ParamArray>] c) = Globals.createElement ("dl", p, c )
        static member dt (p, [<ParamArray>] c) = Globals.createElement ("dt", p, c )
        static member em (p, [<ParamArray>] c) = Globals.createElement ("em", p, c )
        static member embed (p, [<ParamArray>] c) = Globals.createElement ("embed", p, c )
        static member fieldset (p, [<ParamArray>] c) = Globals.createElement ("fieldset", p, c )
        static member figcaption (p, [<ParamArray>] c) = Globals.createElement ("figcaption", p, c )
        static member figure (p, [<ParamArray>] c) = Globals.createElement ("figure", p, c )
        static member footer (p, [<ParamArray>] c) = Globals.createElement ("footer", p, c )
        static member form (p, [<ParamArray>] c) = Globals.createElement ("form", p, c )
        static member h1 (p, [<ParamArray>] c) = Globals.createElement ("h1", p, c )
        static member h2 (p, [<ParamArray>] c) = Globals.createElement ("h2", p, c )
        static member h3 (p, [<ParamArray>] c) = Globals.createElement ("h3", p, c )
        static member h4 (p, [<ParamArray>] c) = Globals.createElement ("h4", p, c )
        static member h5 (p, [<ParamArray>] c) = Globals.createElement ("h5", p, c )
        static member h6 (p, [<ParamArray>] c) = Globals.createElement ("h6", p, c )
        static member head (p, [<ParamArray>] c) = Globals.createElement ("head", p, c )
        static member header (p, [<ParamArray>] c) = Globals.createElement ("header", p, c )
        static member hr (p, [<ParamArray>] c) = Globals.createElement ("hr", p, c )
        static member html (p, [<ParamArray>] c) = Globals.createElement ("html", p, c )
        static member i (p, [<ParamArray>] c) = Globals.createElement ("i", p, c )
        static member iframe (p, [<ParamArray>] c) = Globals.createElement ("iframe", p, c )
        static member img (p, [<ParamArray>] c) = Globals.createElement ("img", p, c )
        static member input (p, [<ParamArray>] c) = Globals.createElement ("input", p, c )
        static member ins (p, [<ParamArray>] c) = Globals.createElement ("ins", p, c )
        static member kbd (p, [<ParamArray>] c) = Globals.createElement ("kbd", p, c )
        static member keygen (p, [<ParamArray>] c) = Globals.createElement ("keygen", p, c )
        static member label (p, [<ParamArray>] c) = Globals.createElement ("label", p, c )
        static member legend (p, [<ParamArray>] c) = Globals.createElement ("legend", p, c )
        static member li (p, [<ParamArray>] c) = Globals.createElement ("li", p, c )
        static member link (p, [<ParamArray>] c) = Globals.createElement ("link", p, c )
        static member main (p, [<ParamArray>] c) = Globals.createElement ("main", p, c )
        static member map (p, [<ParamArray>] c) = Globals.createElement ("map", p, c )
        static member mark (p, [<ParamArray>] c) = Globals.createElement ("mark", p, c )
        static member menu (p, [<ParamArray>] c) = Globals.createElement ("menu", p, c )
        static member menuitem (p, [<ParamArray>] c) = Globals.createElement ("menuitem", p, c )
        static member meta (p, [<ParamArray>] c) = Globals.createElement ("meta", p, c )
        static member meter (p, [<ParamArray>] c) = Globals.createElement ("meter", p, c )
        static member nav (p, [<ParamArray>] c) = Globals.createElement ("nav", p, c )
        static member noscript (p, [<ParamArray>] c) = Globals.createElement ("noscript", p, c )
        static member object' (p, [<ParamArray>] c) = Globals.createElement ("object", p, c )
        static member ol (p, [<ParamArray>] c) = Globals.createElement ("ol", p, c )
        static member optgroup (p, [<ParamArray>] c) = Globals.createElement ("optgroup", p, c )
        static member option (p, [<ParamArray>] c) = Globals.createElement ("option", p, c )
        static member output (p, [<ParamArray>] c) = Globals.createElement ("output", p, c )
        static member p (p', [<ParamArray>] c) = Globals.createElement ("p", p', c )
        static member param (p, [<ParamArray>] c) = Globals.createElement ("param", p, c )
        static member pre (p, [<ParamArray>] c) = Globals.createElement ("pre", p, c )
        static member progress (p, [<ParamArray>] c) = Globals.createElement ("progress", p, c )
        static member q (p, [<ParamArray>] c) = Globals.createElement ("q", p, c )
        static member rp (p, [<ParamArray>] c) = Globals.createElement ("rp", p, c )
        static member rt (p, [<ParamArray>] c) = Globals.createElement ("rt", p, c )
        static member ruby (p, [<ParamArray>] c) = Globals.createElement ("ruby", p, c )
        static member s (p, [<ParamArray>] c) = Globals.createElement ("s", p, c )
        static member samp (p, [<ParamArray>] c) = Globals.createElement ("samp", p, c )
        static member script (p, [<ParamArray>] c) = Globals.createElement ("script", p, c )
        static member section (p, [<ParamArray>] c) = Globals.createElement ("section", p, c )
        static member select (p, [<ParamArray>] c) = Globals.createElement ("select", p, c )
        static member small (p, [<ParamArray>] c) = Globals.createElement ("small", p, c )
        static member source (p, [<ParamArray>] c) = Globals.createElement ("source", p, c )
        static member span (p, [<ParamArray>] c) = Globals.createElement ("span", p, c )
        static member strong (p, [<ParamArray>] c) = Globals.createElement ("strong", p, c )
        static member style (p, [<ParamArray>] c) = Globals.createElement ("style", p, c )
        static member sub (p, [<ParamArray>] c) = Globals.createElement ("sub", p, c )
        static member summary (p, [<ParamArray>] c) = Globals.createElement ("summary", p, c )
        static member sup (p, [<ParamArray>] c) = Globals.createElement ("sup", p, c )
        static member table (p, [<ParamArray>] c) = Globals.createElement ("table", p, c )
        static member tbody (p, [<ParamArray>] c) = Globals.createElement ("tbody", p, c )
        static member td (p, [<ParamArray>] c) = Globals.createElement ("td", p, c )
        static member textarea (p, [<ParamArray>] c) = Globals.createElement ("textarea", p, c )
        static member tfoot (p, [<ParamArray>] c) = Globals.createElement ("tfoot", p, c )
        static member th (p, [<ParamArray>] c) = Globals.createElement ("th", p, c )
        static member thead (p, [<ParamArray>] c) = Globals.createElement ("thead", p, c )
        static member time (p, [<ParamArray>] c) = Globals.createElement ("time", p, c )
        static member title (p, [<ParamArray>] c) = Globals.createElement ("title", p, c )
        static member tr (p, [<ParamArray>] c) = Globals.createElement ("tr", p, c )
        static member track (p, [<ParamArray>] c) = Globals.createElement ("track", p, c )
        static member u (p, [<ParamArray>] c) = Globals.createElement ("u", p, c )
        static member ul (p, [<ParamArray>] c) = Globals.createElement ("ul", p, c )
        static member var (p, [<ParamArray>] c) = Globals.createElement ("var", p, c )
        static member video (p, [<ParamArray>] c) = Globals.createElement ("video", p, c )
        static member wbr (p, [<ParamArray>] c) = Globals.createElement ("wbr", p, c )
